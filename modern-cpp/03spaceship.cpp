/*
3-way comparison operator (spaceship) <=>
lhs <=> rhs
Compara lhs y rhs y permite comparar el valor con cero:

(x <=> y) < 0  sii  x < y
(x <=> y) == 0 sii  x, y son iguales/equivalentes
(x <=> y) > 0  sii  x > y
*/

// #include <bits/stdc++.h>
#include <iostream>
#include<vector>
#include<algorithm>

using namespace std;

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}